spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL
    username: sa
    password:

server:
  port: 8080

jwt:
  secret: test-secret-key
  expiration: 86400000
